hi i used h1 as the heading because its bigger

in the p1 i used the font size and font style to change the apearance of the text

on the listing i used ol for alphabetic order
then i used li to define each item
on h2 i used ul to change the font type. then i used li to define each item